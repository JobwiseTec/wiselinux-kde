---
# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json

type: dnf

repos:
  cleanup: true

install:
  packages:
    - micro
    - btrfs-assistant

remove:
  packages:
    - firefox
    - firefox-langpacks
    - kwrite
